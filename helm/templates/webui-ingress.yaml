# Entry point: ollama.kube.local → AnythingLLM
kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: ollama-webui
  namespace: ollama
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: ollama.kube.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: anythingllm
                port:
                  number: 3001